package refrigerator.back.global.image;

/**
 * generateImageUrl(ImageGenerator generator) : 이미지 생성기를 받아 이미지 url 생성
 */
public abstract class Image {
    abstract public void generateImageUrl(ImageGenerator generator);
}
